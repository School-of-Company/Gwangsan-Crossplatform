diff --git a/node_modules/react-native-keyboard-controller/lib/commonjs/components/KeyboardAvoidingView/index.js b/node_modules/react-native-keyboard-controller/lib/commonjs/components/KeyboardAvoidingView/index.js
index cd3f692..a62346e 100644
--- a/node_modules/react-native-keyboard-controller/lib/commonjs/components/KeyboardAvoidingView/index.js
+++ b/node_modules/react-native-keyboard-controller/lib/commonjs/components/KeyboardAvoidingView/index.js
@@ -57,17 +57,6 @@ const KeyboardAvoidingView = /*#__PURE__*/(0, _react.forwardRef)(({
   const {
     height: screenHeight
   } = (0, _hooks.useWindowDimensions)();
-  const relativeKeyboardHeight = (0, _react.useCallback)(() => {
-    "worklet";
-
-    const keyboardY = screenHeight - keyboard.heightWhenOpened.value - keyboardVerticalOffset;
-    return Math.max(frame.value.y + frame.value.height - keyboardY, 0);
-  }, [screenHeight, keyboardVerticalOffset]);
-  const interpolateToRelativeKeyboardHeight = (0, _react.useCallback)(value => {
-    "worklet";
-
-    return (0, _reactNativeReanimated.interpolate)(value, [0, 1], [0, relativeKeyboardHeight()]);
-  }, [relativeKeyboardHeight]);
   const onLayoutWorklet = (0, _react.useCallback)(layout => {
     "worklet";
 
@@ -103,9 +92,11 @@ const KeyboardAvoidingView = /*#__PURE__*/(0, _react.forwardRef)(({
     if (!enabled) {
       return {};
     }
-    const bottom = interpolateToRelativeKeyboardHeight(keyboard.progress.value);
-    const translateY = interpolateToRelativeKeyboardHeight(translate.value);
-    const paddingBottom = interpolateToRelativeKeyboardHeight(padding.value);
+    const keyboardY = screenHeight - keyboard.heightWhenOpened.value - keyboardVerticalOffset;
+    const relKbHeight = Math.max(frame.value.y + frame.value.height - keyboardY, 0);
+    const bottom = keyboard.progress.value * relKbHeight;
+    const translateY = translate.value * relKbHeight;
+    const paddingBottom = padding.value * relKbHeight;
     const height = frame.value.height - bottom;
     switch (behavior) {
       case "height":
@@ -134,7 +125,7 @@ const KeyboardAvoidingView = /*#__PURE__*/(0, _react.forwardRef)(({
       default:
         return {};
     }
-  }, [behavior, enabled, interpolateToRelativeKeyboardHeight]);
+  }, [behavior, enabled, screenHeight, keyboardVerticalOffset]);
   const combinedRef = (0, _useCombinedRef.default)(internalRef, ref);
   const isPositionBehavior = behavior === "position";
   const containerStyle = isPositionBehavior ? contentContainerStyle : style;
diff --git a/node_modules/react-native-keyboard-controller/src/components/KeyboardAvoidingView/index.tsx b/node_modules/react-native-keyboard-controller/src/components/KeyboardAvoidingView/index.tsx
index ec2ee29..0d3fd6a 100644
--- a/node_modules/react-native-keyboard-controller/src/components/KeyboardAvoidingView/index.tsx
+++ b/node_modules/react-native-keyboard-controller/src/components/KeyboardAvoidingView/index.tsx
@@ -1,7 +1,6 @@
 import React, { forwardRef, useCallback, useMemo } from "react";
 import { View } from "react-native";
 import Reanimated, {
-  interpolate,
   runOnUI,
   useAnimatedStyle,
   useDerivedValue,
@@ -99,23 +98,6 @@ const KeyboardAvoidingView = forwardRef<
     const keyboard = useKeyboardAnimation();
     const { height: screenHeight } = useWindowDimensions();
 
-    const relativeKeyboardHeight = useCallback(() => {
-      "worklet";
-
-      const keyboardY =
-        screenHeight - keyboard.heightWhenOpened.value - keyboardVerticalOffset;
-
-      return Math.max(frame.value.y + frame.value.height - keyboardY, 0);
-    }, [screenHeight, keyboardVerticalOffset]);
-    const interpolateToRelativeKeyboardHeight = useCallback(
-      (value: number) => {
-        "worklet";
-
-        return interpolate(value, [0, 1], [0, relativeKeyboardHeight()]);
-      },
-      [relativeKeyboardHeight],
-    );
-
     const onLayoutWorklet = useCallback(
       (layout: LayoutRectangle) => {
         "worklet";
@@ -169,11 +151,11 @@ const KeyboardAvoidingView = forwardRef<
         return {};
       }
 
-      const bottom = interpolateToRelativeKeyboardHeight(
-        keyboard.progress.value,
-      );
-      const translateY = interpolateToRelativeKeyboardHeight(translate.value);
-      const paddingBottom = interpolateToRelativeKeyboardHeight(padding.value);
+      const keyboardY = screenHeight - keyboard.heightWhenOpened.value - keyboardVerticalOffset;
+      const relKbHeight = Math.max(frame.value.y + frame.value.height - keyboardY, 0);
+      const bottom = keyboard.progress.value * relKbHeight;
+      const translateY = translate.value * relKbHeight;
+      const paddingBottom = padding.value * relKbHeight;
       const height = frame.value.height - bottom;
 
       switch (behavior) {
@@ -202,7 +184,7 @@ const KeyboardAvoidingView = forwardRef<
         default:
           return {};
       }
-    }, [behavior, enabled, interpolateToRelativeKeyboardHeight]);
+    }, [behavior, enabled, screenHeight, keyboardVerticalOffset]);
     const combinedRef = useCombinedRef(internalRef, ref);
     const isPositionBehavior = behavior === "position";
     const containerStyle = isPositionBehavior ? contentContainerStyle : style;
